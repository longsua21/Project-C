#include <stdio.h>

int main() {
    int i, n, tong = 0;

    printf("Nhap 10 so nguyen:\n");
    for (i = 1; i <= 10; i++) {
        printf("So thu %d: ", i);
        scanf("%d", &n);
        tong += n;
    }

    printf("Tong cua 10 so nguyen la: %d\n", tong);

    return 0;
}
